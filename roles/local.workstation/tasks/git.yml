- name: Install git
  apt:
    name: git
    state: latest 
    update_cache: yes

- name: gitconfig
  become_user: "{{workstation_user}}"
  copy:
    src: templates/gitconfig
    dest: "{{workstation_home}}/.gitconfig"
